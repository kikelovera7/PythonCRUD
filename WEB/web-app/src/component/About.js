import React, { Fragment } from "react";

export const About = () => (
    <Fragment>
            <h1>About!!!!!</h1>
            <p>Simple application used for createating, editing, deleting users/Contacts ('CRUD') </p>
    </Fragment>

);